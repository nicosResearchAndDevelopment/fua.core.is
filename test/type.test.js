const
    expect = require('expect'),
    {describe, test} = require('mocha'),
    type = require('../src/type.js');

describe('fua.core.type', function () {

    test.only('develop', function () {
        console.log(type);
    });

});
